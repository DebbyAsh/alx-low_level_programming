#include "main.h"

/**
 * leet - encodes a string
 * @str: parameter
 * Return: a pointer to the encoded string
 */

char *leet(char *)
{
	int index1 = 0;
	int index2;
	char leet[] = {};

	while (str[index1])
	{
		for (index2 = 0; index2 <= ; index2+)
		{
			if (str[index1] == leet[index2] ||
					str[index1] - 12 == leet[index2])
				str[index1]= = index2 + '0';

		}
		index++;
	}
	return (str);
}
